window.QUIZ_DAY_DATA = {
  title: "Day 15 — Aggregate Functions & GROUP BY",
  questions: [
    { q: "What is the difference between COUNT(*) and COUNT(column)?", options: ["They are identical","COUNT(*) counts all rows; COUNT(column) skips NULLs in that column","COUNT(column) counts all rows; COUNT(*) skips NULLs","Neither counts NULLs"], answer: 1, explanation: "COUNT(*) counts every row regardless of NULLs. COUNT(column) only counts non-NULL values in that column." },
    { q: "What does GROUP BY do?", options: ["Sorts rows alphabetically","Groups rows sharing the same column value, for per-group aggregation","Deletes duplicate rows","Filters rows by condition"], answer: 1, explanation: "GROUP BY organizes rows into groups based on a shared value, so aggregates calculate per group." },
    { q: "Why must every non-aggregated column in SELECT also appear in GROUP BY?", options: ["It's optional, just a style choice","MySQL needs to know how to group rows for non-aggregated columns","It speeds up the query","It's only required for COUNT(*)"], answer: 1, explanation: "Without including the column in GROUP BY, MySQL has no defined value to show per group for it." },
    { q: "What does AS do, e.g. COUNT(*) AS total?", options: ["Filters rows","Renames the output column","Sorts results","Creates a new table"], answer: 1, explanation: "AS creates an alias, renaming a column in the query's output for readability." },
    { q: "In what order does MySQL apply WHERE versus GROUP BY?", options: ["GROUP BY first, then WHERE","WHERE first, then GROUP BY","They run simultaneously","Order doesn't matter"], answer: 1, explanation: "WHERE filters rows first; GROUP BY then organizes the remaining rows into groups." },
    { q: "What pattern answers 'which category has the most X'?", options: ["WHERE + ORDER BY","GROUP BY + aggregate + ORDER BY + LIMIT 1","HAVING alone","SELECT * FROM table"], answer: 1, explanation: "Grouping, aggregating, sorting by the aggregate, and limiting to 1 finds the top category." },
    { q: "What does SELECT COUNT(*) FROM students; return if there are 5 rows?", options: ["0","1","5","Error"], answer: 2, explanation: "COUNT(*) returns the total number of rows, which is 5." },
    { q: "What does this return?\n\nSELECT course, COUNT(*) FROM students GROUP BY course;", options: ["One row total, summing everything","One row per unique course, each with its own count","An error","Only the first course"], answer: 1, explanation: "GROUP BY creates one summary row per unique value in the grouped column." },
    { q: "What is wrong with this query?\n\nSELECT course, full_name, COUNT(*) FROM students GROUP BY course;", options: ["Nothing is wrong","full_name isn't aggregated or included in GROUP BY, causing ambiguity/error","COUNT(*) can't be used with GROUP BY","course should not appear in SELECT"], answer: 1, explanation: "full_name must either be aggregated or included in GROUP BY for MySQL to know which value to display." },
    { q: "Write a query to find the maximum age in the students table.", options: ["SELECT MAX(age) FROM students;","SELECT TOP(age) FROM students;","SELECT age LIMIT 1;","SELECT HIGHEST(age) FROM students;"], answer: 0, explanation: "MAX(age) returns the largest value in the age column." },
    { q: "Write a query for total students per course, only counting students older than 20.", options: ["SELECT course, COUNT(*) FROM students WHERE age > 20 GROUP BY course;","SELECT course, COUNT(*) FROM students GROUP BY course WHERE age > 20;","SELECT course FROM students HAVING age > 20;","SELECT COUNT(*) FROM students;"], answer: 0, explanation: "WHERE filters rows before grouping, correctly placed before GROUP BY." },
    { q: "What does this return?\n\nSELECT course, AVG(age) AS avg_age FROM students GROUP BY course ORDER BY avg_age DESC LIMIT 1;", options: ["Every course's average age","Only the course with the highest average age","Only the course with the lowest average age","An error"], answer: 1, explanation: "Sorting descending by average age and limiting to 1 returns the single highest-average course." },
    { q: "True or False: aggregate functions like SUM() and AVG() automatically ignore NULL values.", options: ["True","False"], answer: 0, explanation: "SUM, AVG, MIN, MAX, and COUNT(column) all skip NULLs automatically." },
    { q: "What does MIN(age) return?", options: ["The largest age value","The smallest age value","The average age","The count of ages"], answer: 1, explanation: "MIN() returns the smallest value found in the specified column." },
    { q: "Which clause would you add to filter by AVG(age) after grouping?", options: ["WHERE","GROUP BY","HAVING","ORDER BY"], answer: 2, explanation: "HAVING filters groups based on aggregate results, since WHERE cannot use aggregate functions." }
  ]
};
