window.QUIZ_DAY_DATA = {
  title: "Day 20 — The HAVING Clause",
  questions: [
    { q: "What is the core difference between WHERE and HAVING?", options: ["No difference","WHERE filters rows before grouping; HAVING filters groups after aggregation","HAVING filters rows; WHERE filters groups","Both only work with COUNT(*)"], answer: 1, explanation: "WHERE acts on individual rows pre-grouping; HAVING acts on groups post-aggregation." },
    { q: "Why does 'WHERE COUNT(*) > 2' cause an error?", options: ["COUNT(*) is invalid SQL","WHERE runs before grouping, so COUNT(*) doesn't exist yet at that point","WHERE can't use numbers","2 must be in quotes"], answer: 1, explanation: "Aggregate values aren't calculated until after grouping, so WHERE can't reference them." },
    { q: "Which clause filters based on an aggregate function like AVG() or SUM()?", options: ["WHERE","HAVING","ORDER BY","LIMIT"], answer: 1, explanation: "HAVING is designed specifically to filter on aggregate results." },
    { q: "In a query using WHERE, GROUP BY, and HAVING, what is the required order?", options: ["HAVING, GROUP BY, WHERE","WHERE, GROUP BY, HAVING","GROUP BY, WHERE, HAVING","Any order works"], answer: 1, explanation: "WHERE must come before GROUP BY, and HAVING must come after it." },
    { q: "Can a single query use both WHERE and HAVING?", options: ["No, never","Yes — WHERE filters rows first, HAVING filters resulting groups after","Only WHERE can be used, never both","Only in MySQL 9+"], answer: 1, explanation: "They serve different purposes and can be combined in the same query." },
    { q: "If you only need to filter on a plain column, which clause should you use?", options: ["HAVING, always","WHERE, since it's more efficient and filters before grouping","GROUP BY","Neither is needed"], answer: 1, explanation: "WHERE is the standard, more efficient choice for filtering plain (non-aggregate) conditions." },
    { q: "What does this return?\n\nSELECT course, COUNT(*) FROM students GROUP BY course HAVING COUNT(*) > 2;", options: ["Every course","Only courses with more than 2 students","Only courses with exactly 2 students","An error"], answer: 1, explanation: "HAVING filters out groups whose count doesn't exceed 2." },
    { q: "What is wrong with: SELECT course, AVG(age) FROM students WHERE AVG(age) > 20 GROUP BY course;", options: ["Nothing is wrong","AVG(age) is used in WHERE, but aggregates aren't available until after grouping","GROUP BY is in the wrong place","AVG() doesn't exist in MySQL"], answer: 1, explanation: "The fix is to move the condition to HAVING, placed after GROUP BY." },
    { q: "What does this return?\n\nSELECT course, COUNT(*) FROM students WHERE age > 19 GROUP BY course HAVING COUNT(*) >= 2;", options: ["Filters rows first, then groups, then keeps groups with 2+ remaining students","Filters groups first, then rows","Ignores WHERE entirely","Causes an error"], answer: 0, explanation: "WHERE removes rows first, then GROUP BY forms groups, then HAVING filters those groups." },
    { q: "Write a query to find courses with fewer than 2 students.", options: ["SELECT course, COUNT(*) FROM students GROUP BY course HAVING COUNT(*) < 2;","SELECT course WHERE COUNT(*) < 2;","SELECT course, COUNT(*) FROM students WHERE COUNT(*) < 2;","SELECT * FROM students LIMIT 2;"], answer: 0, explanation: "HAVING correctly filters the grouped counts after aggregation." },
    { q: "Write a query to find courses where MIN(age) is under 20.", options: ["SELECT course, MIN(age) FROM students GROUP BY course HAVING MIN(age) < 20;","SELECT course WHERE MIN(age) < 20;","SELECT MIN(age) FROM students WHERE age < 20;","SELECT course, age FROM students HAVING age < 20;"], answer: 0, explanation: "HAVING filters groups by their MIN(age) result, correctly placed after GROUP BY." },
    { q: "True or False: HAVING can meaningfully be used without GROUP BY.", options: ["True, it's common","False — HAVING is designed to filter the groups GROUP BY creates"], answer: 1, explanation: "Without grouping, there's nothing meaningful for HAVING to filter." },
    { q: "What is the correct full clause order for a query with all of SELECT, FROM, WHERE, GROUP BY, HAVING, ORDER BY, LIMIT?", options: ["SELECT FROM WHERE GROUP BY HAVING ORDER BY LIMIT","SELECT FROM GROUP BY WHERE HAVING LIMIT ORDER BY","SELECT HAVING FROM WHERE GROUP BY ORDER BY LIMIT","FROM SELECT WHERE HAVING GROUP BY LIMIT ORDER BY"], answer: 0, explanation: "This exact sequence is required and cannot be rearranged in standard SQL." },
    { q: "What does HAVING filter that WHERE cannot?", options: ["Text values","Groups based on aggregate results like COUNT or AVG","Numeric values only","Nothing different"], answer: 1, explanation: "HAVING is specifically built to filter on aggregate function results after grouping." },
    { q: "What memory trick helps remember WHERE vs. HAVING order?", options: ["H comes before W alphabetically, so HAVING goes first","W (WHERE) before G (GROUP BY); H (HAVING) after G","They're interchangeable","WHERE always goes last"], answer: 1, explanation: "WHERE precedes GROUP BY; HAVING follows GROUP BY — matching the required clause order." }
  ]
};
