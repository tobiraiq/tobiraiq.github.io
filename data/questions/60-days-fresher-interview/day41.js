window.QUIZ_DAY_DATA = {
  title: "Day 41 — Review: SQL Mixed Practice",
  questions: [
    { q: "Write a query to find students with a score above 80, sorted highest first.", options: ["SELECT * FROM students ORDER BY score;","SELECT full_name, score FROM students WHERE score > 80 ORDER BY score DESC;","SELECT full_name FROM students HAVING score > 80;","SELECT * FROM students GROUP BY score > 80;"], answer: 1, explanation: "WHERE filters rows, ORDER BY score DESC sorts highest first." },
    { q: "Count how many students are enrolled in each course.", options: ["SELECT COUNT(*) FROM students;","SELECT course, COUNT(*) AS total FROM students GROUP BY course;","SELECT course FROM students;","SELECT * FROM students GROUP BY course;"], answer: 1, explanation: "GROUP BY course + COUNT(*) gives one count per unique course." },
    { q: "Find courses with more than 1 student aged 20+.", options: ["SELECT course FROM students WHERE age >= 20;","SELECT course, COUNT(*) FROM students WHERE age >= 20 GROUP BY course HAVING COUNT(*) > 1;","SELECT course FROM students HAVING age >= 20;","SELECT * FROM students WHERE age >= 20 AND COUNT(*) > 1;"], answer: 1, explanation: "WHERE filters rows first (age >= 20), GROUP BY groups them, HAVING filters groups." },
    { q: "What is the correct pattern for LEFT JOIN to find unmatched rows?", options: ["LEFT JOIN + WHERE right.col = NULL","LEFT JOIN + WHERE right.col IS NULL","LEFT JOIN without WHERE","INNER JOIN + WHERE right.col IS NULL"], answer: 1, explanation: "IS NULL (not = NULL) correctly tests for NULL in the right table's columns after a LEFT JOIN." },
    { q: "Find students with the highest score using a subquery.", options: ["SELECT * FROM students ORDER BY score LIMIT 1;","SELECT full_name, score FROM students WHERE score = (SELECT MAX(score) FROM students);","SELECT MAX(score) FROM students;","SELECT full_name WHERE score = MAX(score);"], answer: 1, explanation: "The subquery returns MAX(score); the outer WHERE matches exactly those students." },
    { q: "What clause filters groups after aggregation?", options: ["WHERE","GROUP BY","HAVING","ORDER BY"], answer: 2, explanation: "HAVING filters on aggregated results; WHERE filters individual rows before grouping." },
    { q: "Rank students by score (highest first) using RANK().", options: ["SELECT name, RANK(score) FROM students;","SELECT full_name, score, RANK() OVER(ORDER BY score DESC) AS rnk FROM students WHERE score IS NOT NULL;","SELECT full_name, RANK() FROM students ORDER BY score;","RANK() needs GROUP BY first"], answer: 1, explanation: "RANK() OVER(ORDER BY score DESC) assigns ranks without collapsing rows." },
    { q: "Show all students' scores, replacing NULL with 0.", options: ["SELECT full_name, NVL(score, 0) FROM students;","SELECT full_name, COALESCE(score, 0) AS score FROM students;","SELECT full_name, score = 0 FROM students;","UPDATE students SET score = 0 WHERE score IS NULL;"], answer: 1, explanation: "COALESCE(score, 0) returns score if not NULL, or 0 if NULL." },
    { q: "Find students studying courses that have more than one student enrolled (subquery).", options: ["SELECT * FROM students WHERE COUNT(*) > 1;","SELECT full_name, course FROM students WHERE course IN (SELECT course FROM students GROUP BY course HAVING COUNT(*) > 1);","SELECT * FROM students HAVING course > 1;","SELECT * FROM students JOIN (SELECT course FROM students);"], answer: 1, explanation: "IN with a subquery checks each student's course against the list of qualifying courses." },
    { q: "What does this query return?\nSELECT course, COUNT(*) AS n FROM students WHERE score IS NOT NULL GROUP BY course HAVING n >= 2 ORDER BY n DESC;", options: ["All students","Courses where 2+ students have a recorded score, highest count first","Students with score >= 2","Courses with 2+ students regardless of score"], answer: 1, explanation: "WHERE removes NULL scores first; GROUP BY and HAVING keep courses with 2+ remaining students." },
    { q: "What is the correct clause order for a query using WHERE, GROUP BY, HAVING, ORDER BY?", options: ["GROUP BY, WHERE, HAVING, ORDER BY","WHERE, GROUP BY, HAVING, ORDER BY","HAVING, WHERE, GROUP BY, ORDER BY","ORDER BY, WHERE, GROUP BY, HAVING"], answer: 1, explanation: "The required SQL clause order is SELECT FROM WHERE GROUP BY HAVING ORDER BY LIMIT." },
    { q: "True or False: HAVING can use aggregate functions like COUNT(*) and AVG().", options: ["True","False"], answer: 0, explanation: "HAVING is specifically designed for filtering on aggregate results — WHERE cannot do this." },
    { q: "What is a correlated subquery?", options: ["A subquery that returns two columns","A subquery that references a column from the outer query, re-running for each outer row","A subquery inside FROM","A subquery with GROUP BY"], answer: 1, explanation: "Correlated subqueries re-execute per outer row — used to compare each row to its group's aggregate." },
    { q: "Which window function assigns unique sequential numbers even to tied values?", options: ["RANK()","DENSE_RANK()","ROW_NUMBER()","COUNT() OVER()"], answer: 2, explanation: "ROW_NUMBER() always assigns unique numbers; RANK() and DENSE_RANK() share numbers for ties." },
    { q: "What does COALESCE(a, b, c) return?", options: ["The average of a, b, c","The first non-NULL value from the list","NULL if any argument is NULL","The last value always"], answer: 1, explanation: "COALESCE returns the first non-NULL value scanning left to right." }
  ]
};
