window.QUIZ_DAY_DATA = {
  title: "Day 58 — Full Mock Interview",
  questions: [
    { q: "What is the purpose of running a mock interview on Day 58?", options: ["To practice failure","To identify exactly which moments feel smooth and which feel uncertain, so Days 59-60 can be targeted precisely","To memorise perfect answers","To impress a peer"], answer: 1, explanation: "The mock is diagnostic — not pass/fail. Its value is in revealing the specific gaps, not in being 'right' for every question." },
    { q: "Write the function for T2: find a pair of numbers in a list that sum to a target.", options: ["def find_pair(nums, t): return [x for x in nums if x == t]","def find_pair(nums, target):\n    seen = set()\n    for n in nums:\n        if target - n in seen: return (target-n, n)\n        seen.add(n)\n    return None","def find_pair(nums, target): return (nums[0], nums[1])","def find_pair(nums, t): return max(nums)"], answer: 1, explanation: "The seen-set approach is O(n): for each number, check if its complement (target - n) has already been seen. This finds the pair in one pass." },
    { q: "What does y = x do when x is a list, and why does y.append(4) also change x?", options: ["y becomes an independent copy of x","y = x creates a second name pointing to the same list object — both x and y reference the identical list in memory, so any mutation affects both","y only holds a reference to x's length","x is reset when y is assigned"], answer: 1, explanation: "In Python, assignment with a mutable object (list, dict) does NOT copy it. Both names point to the same object. Use x.copy() or list(x) to get an independent copy." },
    { q: "What is the correct SQL query for T6: avg salary per dept, only where avg > 70000?", options: ["SELECT dept, AVG(salary) FROM employees WHERE AVG(salary) > 70000;","SELECT department, AVG(salary) AS avg_salary FROM employees GROUP BY department HAVING AVG(salary) > 70000;","SELECT dept, salary FROM employees GROUP BY dept HAVING salary > 70000;","SELECT AVG(salary) FROM employees WHERE salary > 70000 GROUP BY dept;"], answer: 1, explanation: "GROUP BY creates per-department groups; HAVING filters after aggregation. WHERE cannot be used here because AVG() doesn't exist at the WHERE stage." },
    { q: "What is the SQL pattern for T8: second highest salary?", options: ["ORDER BY salary DESC LIMIT 2","SELECT MAX(salary) FROM employees WHERE salary < (SELECT MAX(salary) FROM employees)","SELECT salary FROM employees WHERE RANK() = 2","SELECT SECOND(salary) FROM employees"], answer: 1, explanation: "Exclude the maximum salary with a subquery, then take MAX of what remains. This pattern extends to Nth highest by nesting further." },
    { q: "What does 'green result' mean in Day 58's self-scoring rubric?", options: ["Answering every question perfectly","6/8 technical correct within time + PSET and PATO under 90 seconds + 2 strong HR answers + one good question asked","Passing all 12 technical questions","Getting positive peer feedback on everything"], answer: 1, explanation: "Green = interview-ready, not perfect. 6/8 technical, fluent HR answers under time, and one good question to the interviewer is the realistic benchmark." },
    { q: "What is the Git full team feature workflow in order?", options: ["push → branch → commit → PR → merge","checkout main → pull main → checkout -b feature → code → commit → push → PR → address review → merge → pull main → delete branch","branch → code → commit → push","fork → PR → merge → branch"], answer: 1, explanation: "Always start from an up-to-date main. Branch per feature. Commit with clear messages. Push and open PR. Address review by pushing more commits. After merge, sync main and clean up." },
    { q: "What does a strong BankAccount class's withdraw method check before reducing the balance?", options: ["That amount is a string","That amount is greater than zero and not greater than self.balance — if insufficient, print a message and return False","That the account number is correct","That the bank is open"], answer: 1, explanation: "The guard condition (if amount > self.balance) prevents balance from going negative. Return False on failure so the caller knows the operation didn't succeed." },
    { q: "What is the difference between a technical round and an HR round in terms of what each is evaluating?", options: ["They evaluate the same things","Technical round: can you solve problems and write/explain code. HR round: can you communicate about yourself, your project, and your professional fit","Technical round evaluates personality","HR round tests technical knowledge"], answer: 1, explanation: "Technical round = demonstrate skills. HR round = demonstrate communication, self-awareness, and cultural fit. Prepare differently for each." },
    { q: "In the mock interview, when should you look at the model answers?", options: ["Before starting to set expectations","After attempting every single question — not during the simulation","Whenever you get stuck","At the halfway point"], answer: 1, explanation: "Looking at answers during the simulation defeats its diagnostic purpose. Complete every question first — even imperfectly — then compare to the model answers." },
    { q: "True or False: the peer mock version is better than the solo simulation for most candidates.", options: ["True — external feedback is more accurate than self-assessment","False — solo is always better"], answer: 0, explanation: "A peer can observe what you can't: hesitation before answering, body language, 'um' frequency, and whether explanations actually make sense to a listener." },
    { q: "If your PATO answer in the mock ran 3 minutes, what should go on your Day 60 polish list?", options: ["Memorise the full 3-minute version","Cut one sentence from A and one from T to bring it under 90 seconds, then re-practise","Expand it further","Start a new PATO from scratch"], answer: 1, explanation: "PATO target is 90 seconds. Cutting the longest sentence in A (approach) and T (tech stack) is the fastest fix — these sections typically have the most padding." },
    { q: "What should go on your Day 59 targeted revision list from the mock rubric?", options: ["Every topic in the course","Only the red and yellow areas — questions that took over 6 minutes or concepts you could not recall confidently","Everything you answered correctly","Nothing — Day 59 is a rest day"], answer: 1, explanation: "Targeted review of weak areas is far more effective than re-reading everything. Identify the 2–3 specific gaps and spend 80% of Day 59 there." },
    { q: "What is the LEFT JOIN self join pattern for showing employee + manager name?", options: ["INNER JOIN employees ON manager_id = emp_id","SELECT e.name AS employee, m.name AS manager FROM employees e LEFT JOIN employees m ON e.manager_id = m.emp_id","SELECT name, manager FROM employees","JOIN managers ON managers.id = employees.id"], answer: 1, explanation: "Two aliases (e for employee, m for manager) on the same table. LEFT JOIN keeps employees with no manager (NULL manager_id), showing NULL in the manager column." },
    { q: "What is the difference between git revert and git reset and which is safe for pushed commits?", options: ["They are identical","revert = creates a new undo commit (safe for pushed); reset = rewrites history by moving the branch pointer (only safe for unpushed local commits)","reset is safe for pushed commits","Both rewrite history"], answer: 1, explanation: "revert preserves history with a new commit that undoes the changes — safe for shared branches. reset rewrites history and will conflict with what other developers have already pulled." }
  ]
};
