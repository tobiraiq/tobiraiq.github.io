window.QUIZ_DAY_DATA = {
  title: "Day 25 — LEFT JOIN & RIGHT JOIN",
  questions: [
    { q: "How does LEFT JOIN differ from INNER JOIN?", options: ["They are identical","LEFT JOIN returns ALL left rows plus NULLs for unmatched right rows; INNER JOIN returns matched only","RIGHT JOIN returns ALL left rows","LEFT JOIN always returns more columns"], answer: 1, explanation: "INNER JOIN = matched only. LEFT JOIN = all left + NULLs where no right match." },
    { q: "When does NULL appear in a LEFT JOIN result?", options: ["When the left table is empty","When a left-table row has no matching row in the right table","When both tables have the same data","Never — NULLs are removed by JOIN"], answer: 1, explanation: "NULLs fill the right-side columns for left rows that have no matching right row." },
    { q: "What query pattern finds unmatched rows after a LEFT JOIN?", options: ["WHERE left.col = NULL","WHERE right_table.column IS NULL","HAVING COUNT(*) = 0","WHERE NOT EXISTS"], answer: 1, explanation: "Testing a right-side column for IS NULL after a LEFT JOIN identifies unmatched left rows." },
    { q: "What does a RIGHT JOIN keep?", options: ["All left rows","All right rows; NULLs in left columns where no match","Only matched rows","Nothing — it reverses the JOIN"], answer: 1, explanation: "RIGHT JOIN ensures every right-table row appears, filling left columns with NULL where no match." },
    { q: "Why do most developers prefer LEFT JOIN over RIGHT JOIN?", options: ["LEFT JOIN is faster","Swapping table order turns a RIGHT JOIN into a LEFT JOIN — easier to read","RIGHT JOIN produces more NULLs","RIGHT JOIN is deprecated"], answer: 1, explanation: "Any RIGHT JOIN can be rewritten as a LEFT JOIN by swapping tables — and LEFT JOIN reads more naturally." },
    { q: "How many rows does LEFT JOIN return vs INNER JOIN on the students/courses setup?", options: ["Same number","LEFT returns 5 (all students); INNER returns 4 (Zuri excluded)","LEFT returns 3; INNER returns 5","Both return 5"], answer: 1, explanation: "LEFT JOIN keeps all 5 students including Zuri; INNER JOIN excludes Zuri (NULL course_id)." },
    { q: "Write a query to find students NOT enrolled in any course.", options: ["SELECT * FROM students WHERE course_id = NULL;","SELECT s.full_name FROM students s LEFT JOIN courses c ON s.course_id = c.course_id WHERE c.course_id IS NULL;","SELECT * FROM students INNER JOIN courses ON 1=1;","SELECT full_name FROM students WHERE course IS NOT NULL;"], answer: 1, explanation: "LEFT JOIN then filter WHERE the right-table column IS NULL to find unmatched left rows." },
    { q: "What value shows in the course_name column for Zuri after LEFT JOIN?", options: ["0","Empty string","NULL","Her student_id"], answer: 2, explanation: "When no matching right row exists, the right-side columns show NULL." },
    { q: "True or False: LEFT JOIN always returns at least as many rows as INNER JOIN.", options: ["True","False"], answer: 0, explanation: "LEFT JOIN is a superset — it includes all INNER JOIN matches plus the unmatched left rows." },
    { q: "What does this find?\nSELECT c.course_name FROM students s RIGHT JOIN courses c ON s.course_id = c.course_id WHERE s.student_id IS NULL;", options: ["All students","Courses with no students enrolled","Courses with all students","All courses"], answer: 1, explanation: "RIGHT JOIN keeps all courses; WHERE student IS NULL identifies those with no enrolled students." },
    { q: "Can a RIGHT JOIN always be rewritten as a LEFT JOIN?", options: ["No — they behave completely differently","Yes — by swapping the table order and changing RIGHT to LEFT","Only if both tables are the same size","Only in MySQL 8+"], answer: 1, explanation: "Swapping table positions and changing the JOIN keyword produces identical results." },
    { q: "What clause filters a LEFT JOIN result to only unmatched rows?", options: ["HAVING IS NULL","WHERE right_table.column IS NULL","ORDER BY NULL","GROUP BY NULL"], answer: 1, explanation: "Filter using IS NULL on a non-nullable column from the right table to find the unmatched left rows." },
    { q: "In a LEFT JOIN, which table's rows are guaranteed to all appear?", options: ["The right table","The left (first) table","Neither","Both"], answer: 1, explanation: "LEFT JOIN guarantees every row from the LEFT (first-listed) table appears in the result." },
    { q: "What does RIGHT JOIN fill in the left columns for unmatched rows?", options: ["0","Empty string","NULL","The right table's primary key"], answer: 2, explanation: "Unmatched positions are filled with NULL, regardless of the column's normal data type." },
    { q: "Which JOIN type is best for 'show all products, even those with no orders'?", options: ["INNER JOIN","LEFT JOIN with products on the left","SELF JOIN","CROSS JOIN"], answer: 1, explanation: "LEFT JOIN with products first keeps all products; orders columns show NULL where no order exists." }
  ]
};
