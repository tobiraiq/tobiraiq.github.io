window.QUIZ_DAY_DATA = {
  title: "Day 43 — OOP: Methods, self & Inheritance",
  questions: [
    { q: "What is inheritance in OOP?", options: ["Making a copy of a class","A child class automatically receiving all attributes and methods from a parent class","Creating two objects at once","A method that calls itself"], answer: 1, explanation: "Inheritance enables code reuse — the child starts with everything the parent has." },
    { q: "What does super().__init__() do?", options: ["Deletes the parent class","Calls the parent class's __init__ from inside the child, setting up parent attributes","Creates a new parent object","Replaces self in the child"], answer: 1, explanation: "super().__init__() is required in the child when it adds its own attributes on top of the parent's." },
    { q: "What is method overriding?", options: ["Deleting a parent method","A child class redefining a method with the same name, replacing the parent's behaviour","Calling the parent method twice","Adding self to an inherited method"], answer: 1, explanation: "Override = same method name in child, different implementation." },
    { q: "What is __str__ and why is it useful?", options: ["A string variable","A method returning a readable string when print(obj) is called — instead of showing a memory address","A required method in every class","An alias for __init__"], answer: 1, explanation: "__str__ controls what print(obj) displays — makes objects human-readable." },
    { q: "What is polymorphism in Python OOP?", options: ["A class with many attributes","Different classes implementing the same method name with different behaviour","A method calling itself recursively","A class inheriting from multiple parents"], answer: 1, explanation: "Dog.speak() and Cat.speak() share the name but produce different outputs — classic polymorphism." },
    { q: "What does 'class Dog(Animal):' mean?", options: ["Dog is inside Animal","Dog inherits all attributes and methods from Animal","Animal inherits from Dog","Dog and Animal are the same class"], answer: 1, explanation: "The parent class name in parentheses makes Dog a child of Animal." },
    { q: "What does this print?\nclass A:\n    def greet(self): return 'Hello from A'\nclass B(A): pass\nb = B()\nprint(b.greet())", options: ["Error — B has no greet","Hello from A","Hello from B","None"], answer: 1, explanation: "B inherits greet() from A — calling it on b returns 'Hello from A'." },
    { q: "What does this print?\nclass A:\n    def speak(self): return 'A speaks'\nclass B(A):\n    def speak(self): return 'B speaks'\nb = B()\nprint(b.speak())", options: ["A speaks","B speaks","A speaks then B speaks","Error"], answer: 1, explanation: "B overrides speak(), so its own version is called for B objects." },
    { q: "What is wrong?\nclass Animal:\n    def __init__(self, name): self.name = name\nclass Dog(Animal):\n    def __init__(self, name, breed):\n        self.breed = breed\nd = Dog('Rex','Lab')\nprint(d.name)", options: ["Nothing","AttributeError — super().__init__(name) was not called so self.name was never set","Dog needs its own name attribute","Lab must be an integer"], answer: 1, explanation: "Without super().__init__(name), the parent's __init__ never runs and self.name is never created." },
    { q: "What does this print?\nclass P:\n    def __init__(self, x): self.x=x\n    def __str__(self): return f'x={self.x}'\np = P(10)\nprint(p)", options: ["<P object>","x=10","P(10)","10"], answer: 1, explanation: "__str__ returns 'x=10', which print() uses instead of the default memory address." },
    { q: "True or False: a child class can define methods the parent does not have.", options: ["True","False"], answer: 0, explanation: "A child inherits parent methods AND can add brand new methods of its own." },
    { q: "What OOP concept does Dog, Cat, and Bird each having a speak() method with different outputs demonstrate?", options: ["Encapsulation","Inheritance only","Polymorphism","Abstraction"], answer: 2, explanation: "Same method name, different behaviour per class = polymorphism." },
    { q: "What is a 'dunder method'?", options: ["A method that uses double returns","A special method with double underscores on each side like __init__ or __str__","A deprecated Python 2 feature","A method only available in child classes"], answer: 1, explanation: "Dunder = double underscore. __init__, __str__, __len__ are common dunder/magic methods." },
    { q: "When does Python call __str__ automatically?", options: ["When the object is created","When print(obj) or str(obj) is called","When the object is deleted","When the class is imported"], answer: 1, explanation: "Python calls __str__ automatically during print() and str() operations on an object." },
    { q: "Write a Shape parent with colour and a Circle child that adds radius and an area() method.", options: ["Circle extends Shape with area","class Shape:\n    def __init__(self,colour): self.colour=colour\nclass Circle(Shape):\n    def __init__(self,colour,r):\n        super().__init__(colour); self.r=r\n    def area(self): import math; return math.pi*self.r**2","class Circle(Shape,radius): area()","circle = Shape(); circle.area = lambda: r**2"], answer: 1, explanation: "super().__init__(colour) sets the parent attribute first; then self.r is added; area() computes pi*r^2." }
  ]
};
