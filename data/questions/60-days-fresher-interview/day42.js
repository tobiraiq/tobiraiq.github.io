window.QUIZ_DAY_DATA = {
  title: "Day 42 — Review: Python OOP",
  questions: [
    { q: "What does this print?\nclass Dog:\n    def __init__(self, name): self.name = name\n    def speak(self): return self.name + ' says Woof'\nd = Dog('Rex')\nprint(d.speak())", options: ["says Woof","Rex says Woof","Dog says Woof","Error"], answer: 1, explanation: "self.name is 'Rex'; speak() concatenates it with ' says Woof'." },
    { q: "What is wrong?\nclass Cat:\n    def __init__(name, age):\n        self.name = name", options: ["name should be *name","self is missing as the first parameter","age must have a default value","__init__ should be init"], answer: 1, explanation: "Python requires self as the first parameter of every instance method including __init__." },
    { q: "What does this print?\nclass Animal:\n    species = 'Unknown'\na1 = Animal()\na2 = Animal()\na1.species = 'Dog'\nprint(a1.species)\nprint(a2.species)", options: ["Dog Dog","Unknown Unknown","Dog Unknown","Unknown Dog"], answer: 2, explanation: "a1.species = 'Dog' creates an instance variable shadowing the class variable for a1 only; a2 still reads 'Unknown'." },
    { q: "In a BankAccount class, where should the balance attribute be initialised?", options: ["As a class variable outside __init__","As self.balance = 0 inside __init__","As a global variable","As a function parameter"], answer: 1, explanation: "Instance variables like balance should be set in __init__ with self.balance, making each account's balance independent." },
    { q: "What does this print?\nclass Counter:\n    count = 0\n    def __init__(self): Counter.count += 1\nc1 = Counter()\nc2 = Counter()\nc3 = Counter()\nprint(Counter.count)", options: ["0","1","3","None"], answer: 2, explanation: "Each instantiation calls __init__, incrementing the shared class variable Counter.count — three times gives 3." },
    { q: "Write the area() method for a Rectangle class with self.width and self.height.", options: ["def area(): return width * height","def area(self): return self.width * self.height","def area(width, height): return width * height","area = self.width * self.height"], answer: 1, explanation: "Methods must accept self; attributes are accessed via self.attribute_name." },
    { q: "What OOP pillar is this? A Student class has a name attribute and a study() method — both in one class.", options: ["Inheritance","Polymorphism","Encapsulation","Abstraction"], answer: 2, explanation: "Encapsulation bundles data (attributes) and behaviour (methods) into a single class unit." },
    { q: "What does this print?\nclass Point:\n    def __init__(self, x, y): self.x=x; self.y=y\n    def dist(self): return (self.x**2+self.y**2)**0.5\np=Point(3,4)\nprint(p.dist())", options: ["7","5.0","25","Error"], answer: 1, explanation: "Distance from origin (3,4): sqrt(9+16) = sqrt(25) = 5.0." },
    { q: "A Student class has a scores list. Write the average() method.", options: ["def average(): return sum(scores)/len(scores)","def average(self): return sum(self.scores)/len(self.scores)","def average(scores): return sum/len","average = sum(self.scores)/len(self.scores)"], answer: 1, explanation: "Access the list as self.scores inside the method body." },
    { q: "How many objects can you create from one class?", options: ["Only one — that is what a class is","As many as needed — each is an independent object","Maximum 10","Two: one for each init parameter"], answer: 1, explanation: "A class is a blueprint — you can create unlimited independent objects from it." },
    { q: "What is the difference between a class variable and an instance variable?", options: ["No difference","Class variable: shared by all instances; instance variable: unique per object","Instance variable: shared; class variable: unique","Variables inside __init__ are class variables"], answer: 1, explanation: "Class variables (outside __init__) are shared; instance variables (self.x in __init__) are per-object." },
    { q: "True or False: self is a Python keyword that must always be named 'self'.", options: ["True","False — self is a convention; any name works, but self is universal"], answer: 1, explanation: "The first parameter of instance methods can technically have any name, but 'self' is the universal Python convention." },
    { q: "What does __init__ stand for, and when is it called?", options: ["Initialise — called when you import the class","Initialise — called automatically when a new object is created","Internal init — called once per class definition","It must be called manually"], answer: 1, explanation: "__init__ is Python's constructor, automatically called by Python when you write ClassName(args)." },
    { q: "Write a Vehicle class with make, year attributes and a describe() method returning 'make year'.", options: ["class Vehicle:\n    def describe(): return make + year","class Vehicle:\n    def __init__(self, make, year):\n        self.make=make; self.year=year\n    def describe(self):\n        return f'{self.make} {self.year}'","class Vehicle(make, year): describe()","Vehicle.make = make; Vehicle.year = year"], answer: 1, explanation: "Complete OOP: __init__ stores attributes; describe() accesses them via self." },
    { q: "What does this print?\nclass X:\n    def __init__(self, n): self.n = n\n    def double(self): return self.n * 2\nx = X(7)\nprint(x.double())", options: ["7","14","None","Error"], answer: 1, explanation: "self.n is 7; double() returns 7 * 2 = 14." }
  ]
};
